# Created by .gitignore support plugin (hsz.mobi)
### Node template
# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# Commenting this out is preferred by some people, see
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git-
node_modules

# Users Environment Variables
.lock-wscript

.idea
# transient test artifacts
node_modules/
scratch-*
_t3d_test.js

# tools/headshot.mjs render output
.headshots/

# CMU mocap source files (tools/mocap/fetch.mjs re-downloads them on demand;
# the retargeted clips in tools/motion/ are what the build actually reads)
tools/mocap/cache/
.smoke/

# Studio Ochi source assets are licensed and stay out of the repo. What IS
# committed is the retargeted motion in tools/motion-ochi/, which is derived
# from the free CMU database, plus the tools that rebuild the GLB from both.
*.fbx
*.blend
tools/ochi/
