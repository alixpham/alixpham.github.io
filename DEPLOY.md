# Deploy log

Flagster is served by GitHub Pages from the `master` branch at
**https://alixpham.github.io/**. This file records the commit id behind each
notable live deploy (newest first).

Version tags (see `VERSION`): **v3.12.0** = `PENDING`,
**v3.11.0** = `bc22cbc998b2f8d2ed18e19c58c479af2dccb98c`,
**v3.10.0** = `d3f5d7796aaf9d996c981f4ae4d7a5c01dae11e1`,
**v3.9.0** = `57c46ce3c8e096bb516d8c40741e294fd7099057`,
**v3.8.0** = `9600cc89d126c08136709b633f6780e8cc0a778f`,
**v3.7.0** = `9600cc89d126c08136709b633f6780e8cc0a778f` (shipped in the same squash merge),
**v3.6.0** = `f138013af23650176a361121992c4d895b427c21`,
**v3.5.0** = `d12ada92935e8d4e581a1515ea06a41f8d445dde`,
**v3.4.0** = `4e06d885f0752bb8ce833c78c6be369f9e00c144`,
**v3.3.0** = `6f9c5f4227c4218d8fccb9a6b4259258d966def3`,
**v3.2.0** = `95a99024bb4a8f9af4cfb7834880ec39cf7a56b9`,
**v3.1.0** = `01f52dc6fbfdfaf9b71e2fe7376ccf75b1f7b2f9`,
**v3.0.3** = `5ae6c5b986b01cf5356e0bed71d7ba225ac3e9b0`,
**v3.0.2** = `f6c38c5126a77caae899cc5d7f6a808f5f83d2fd`,
**v3.0.1** = `e4e6f42f989f3f3328fb6fc4efcaa32e4c8d0bb3`,
**v3.0.0** = `29109b795333a8a75a6bfaa1977478d831351c2a`,
**v2.35.0** = `1deb24ff229adeb2b2d2080ac29e3f6214741210`,
**v2.34.0** = `fb86f856d301b50f0597df713059c5fe5c71ce0d`,
**v2.33.0** = `7f8156b5e491284f201ddd11bb58ae2b24c0d319`,
**v2.32.0** = `74eb5a30f14d9c15b7270e107baa4c02fb63c724`,
**v2.31.0** = `23ae387afc601f976906728e3842189a2d29b888`,
**v2.30.0** = `322c6c61be8277ee2c109bd91550e738e4143270`,
**v2.29.0** = `b6b84aa16fd7759270b3dea774ce467f2da753c8`,
**v2.28.2** = `3fd1eb2de55ced6210647ef36932847c682b9f9c`,
**v2.28.1** = `0ca4e985b6b9ffaf591316c198285f73d70f94ee`,
**v2.28.0** = `8350872472145188e16520795a69617e19326f3a`,
**v2.27.0** = `6160c98c6604a5e14064cdb55b0900a186873bcf`,
**v2.26.0** = `6160c98c6604a5e14064cdb55b0900a186873bcf` (same squash — v2.26.0 and v2.27.0 shipped together),
**v2.25.0** = `61605f60aae9f0c56a8530f805fef8afc0e55eb3`,
**v2.24.0** = `929262a37e430d90da2e867944f38e2ce4f8cf72`,
**v2.23.0** = `ccbda0339924ad7a5df9983e00e629e6dbea0aad`,
**v2.22.0** = `202b37231e2a12a11bb95060a7f01ee317be74a7`,
**v2.21.0** = `c9afd5c6cfc0a83c7071f976fdabfe504b965ad5`,
**v2.20.0** = `05687b8205d7f9a98edd38235fb92a2382c6ff7d`,
**v2.19.0** = `64c71c3ba8257d1c30aa3c990fde316ccd23e255`,
**v2.18.0** = `30e093f8b094fba0bc67022ef909f475bba4c950`,
**v2.17.0** = `343b2b4f0b234ea676b3d6e958074420ebd2cbe5`,
**v2.16.0** = `f59df70bf4a0ea48d6717cceea9fc1c9c67a55df`,
**v2.15.0** = `d07abdf02733e964732b082e54d2c29f55be2479`,
**v2.14.0** = `138824a64a86bab02547157ec66af9677c1b74dd`,
**v2.13.0** = `06d78f79528a462b639b0dcba17c24c0d1114eca`,
**v2.12.0** = `92a7c025f5561ac10a4189d933fcb9ad73f186cc`,
**v2.11.0** = `176af3e40a171bf9df71f9a1e653fcc56ba8ef6f`,
**v2.10.0** = `745131ce16f2307dbc53f477efa35ac676c598a7`,
**v2.9.0** = `0cc4900b4c3590fbd91065d3e419951e383ba6ce`,
**v2.8.0** = `0111346b16523e61752e9d59fc289a1c81136527`,
**v2.7.0** = `a73fc4a3919275213a2ffe522621249c7d68a8fa`,
**v2.6.0** = `9bdbdc10e6f10d70f55b52083a8c8495439270a6`,
**v2.5.0** = `01cffb2ae102abab864c9c791b65914a229e3c62`,
**v2.4.0** = `1f26722c7c368f849d1ec74f9a1f0ad83164b476`,
**v2.3.0** = `243074430c9bc2db549b82f9576abc015f5e60d5`,
**v2.2.0** = `ee8883d78e5fb19ca925c7332cf351fc88bd5194`,
**v2.1.1** = `d44e50d5f9a89b7ed15c49193b6700bd3232397c`,
**v2.1.0** = `8f8804f57a644dcea6ba8b997aee2a77d0d9c1d0`,
**v2.0.0** = `323d1906391a50cdc257d1ed746f04994e6751df`,
**v1.9.1** = `badcf47c09151d8f2d797b5f2971c819898d3dd5`,
**v1.9.0** = `bedc17d4f761528f9f3a5c0b34684d925efed50d`,
**v1.8.0** = `ee9ff9211e7eda1a8571ec634e1affc570cc560f`,
**v1.7.0** = `bddb3464388181f4aa13aaa8f97d5985f0da30c3`,
**v1.6.0** = `2dcd368a81cfa1c4490f6fd655085a36fd890a24`,
**v1.5.1** = `6df11a364453db54a6255a4ad9addeaa2062978f`,
**v1.5.0** = `d11b13ecd1ce6782b0200c8be38764b2a5b5e429`,
**v1.4.1** = `bf76d1ab6c6b239709ca936323a2acd854d0e0c9`,
**v1.4.0** = `6447c6a990f53d0e95ae17b75949cfa2ce17b4cf`,
**v1.3.0** = `037017902a4b22e829fa7d2e862196258e8f8d6e`,
**v1.2.0** = `c72c2d1aa16bf43f0dfb85e76bfddc2458899fe8`,
**v1.1.0** = `e8b079ae1412b079b5411b086b86f033345f7fec`,
**v1.0.0** = `024f9a6b5c652ded9617add74bf5d54008ffda7d`. (Git tags exist locally but the
git proxy blocks tag pushes, so these are the authoritative version→commit records.)

| Date (UTC) | Commit | What shipped |
| --- | --- | --- |
| 2026-08-27 | `PENDING` | **v3.12.0** — the squad was hovering. *"Make sure gravity works but players are light on their feet."* Gravity, checked first: the ball has it in flight and on the ground (`_updateLoose` bounces and rolls it), the pelvis bobs 6.6-27.7cm depending on the gait, the swing foot clears 12cm walking and 65cm sprinting, and no clip but Dive — which is meant to — puts a vertex under the turf. **The engine has no vertical physics for a PLAYER at all**: every centimetre of a player's rise and fall is the animation plus `PLAYER_LIFT`, one constant that raises the holder because the rig dips below its own origin. Measured off the renderer with the new `tools/footcheck.mjs` (`npm run feet`), that constant was **3.5cm too big — the whole squad planted between 2.5 and 4.8cm above the grass**. 0.10 -> 0.065 puts them on it: two runs give a mean plant of +1.43cm and -0.60cm, straddling the turf with about a centimetre of run-to-run noise, and chasing the residual would be fitting that noise. Getting to that number took two wrong readings, both instructive and both now written into the probe. Polling from OUTSIDE the render loop caught players mid-flight and never in stance — swiftshader draws twice a second and the airborne half of a stride is half of those — and reported them 4cm up for the opposite reason. Taking each player's lowest point EVER counted the frame he dived, which is meant to go thirty centimetres down, and reported contact on zero. The honest metric is the tenth percentile of a player's own per-frame low. Also fixed, found while measuring: `player3d.js` normalised a loaded model's height with `2.385 / 1.850`, where 1.850 is the height the GAME'S rig happens to be authored at — a per-character constant pretending to be universal. It silently overrode the `authorHeight` normalisation `playermodel.js` does for exactly this reason and rendered the 1.744m Studio Ochi athlete **6% short**; dividing by the model's own height puts both characters at the same size on the field, measured 1.998 against 2.001 units. On "light on their feet": nothing measured says they are heavy. Swing clearance is healthy at every gait, the contact foot's lateral slide (3-12cm against a 50-93cm stride) matches the game's own model, and the momentum model already scales the gait ladder per player through `setBuildScale`. No agility constant was touched on a hunch. |
| 2026-08-27 | `bc22cbc998b2f8d2ed18e19c58c479af2dccb98c` | **v3.11.0** — the player-model toggle is hidden. The menu carried a "Player: Ochi / Classic" button while the imported character was being proved out; it is not a choice anyone should have to make, so it is gone from the footer. The machinery stays: `playermodel.js` still knows both characters and still falls back to the classic one when `ochiplayer.glb` is missing, and `?character=flagplayer` still switches for a build check. **The stored preference is deliberately no longer read.** The button wrote the choice to localStorage, so honouring it after hiding the control would strand anyone who had switched to the classic player on it with no visible way back — a worse failure than losing a setting nobody was asked to make. Verified all three ways: a fresh visitor gets Ochi, a visitor carrying `flagster.character = "flagplayer"` from the old button ALSO gets Ochi, and the URL override still works; no toggle in the menu footer and 0 page errors in any of them. |
| 2026-08-27 | `d3f5d7796aaf9d996c981f4ae4d7a5c01dae11e1` | **v3.10.0** — the quarterback's read, and the ball in his hands. Reported from play: *"QB should pass and handoff more to open players because risk interception"* and *"check the ball holding by players."* Both needed an instrument before they needed a fix. **(1) The read was taken at the wrong instant.** `_aiThrow` ranked receivers by the distance to the nearest defender *at the moment of the decision*, and the ball does not arrive at that moment: the wind-up runs 374ms and the flight another 0.7s, through which every defender closes at up to nine yards a second. `tools/qbstats.mjs` (`npm run qb`) freezes the field at every CPU throw and measures it — the quarterback read **4.62 yards** of separation and the ball landed in **1.87**, and **41.5% of throws arrived with under a yard of it**. Worse, the read *order* was the whole decision: the loop walked WR1, WR2, RB, C and threw to the first man over the bar, so nobody after him was ever looked at and the centre — read four — **caught 0.0% of passes in his existence**. `_readReceiver` now runs the same lead solve `_releaseThrow` runs and measures separation AT THE ARRIVAL POINT, giving each defender the flight time (less the beat it takes to read the release) to close on it at his own speed; every read is scored in those units and the best one wins, with the progression surviving as what it really is — a yard of separation on read one, a quarter of one on read four. Two things had to be priced in beside openness and the first attempt got both wrong: handing defenders the wind-up as well as the flight made every downfield throw look covered and the quarterback threw the ball an average of **0.06 yards BEHIND the line**, and separation had to be **capped**, because a man three yards clear and a man eight yards clear are the same catch and ranking them apart is why the flat always won — nobody covers the place the play is not going. Throws arriving into coverage **41.5% -> 9.7%**, throws into a lane with a defender in front **73.7% -> 35.8%**, completion **50.2% -> 60.5%** (in band), yards per pass play 7.25 -> 7.39, time to throw 2.00s -> 2.41s, across three seeds. Interceptions fall 3.1% -> 2.0%, under their ~3-5% band: that is v2.15.0's trade reversed — it shipped completions below band *on purpose* because "the quarterback forces throws under pressure now, which is what pays for the interceptions" — and if the band matters more, the honest knob is the catch model rather than making him throw badly again. A CPU outlet pitch was written and then **measured out of the build**: over 471 plays there was a man behind the passer on **zero** of 5,721 read-frames, because every route in the playbook goes forward. **(2) The football was not in anybody's hands.** `tools/ballcheck.mjs` (`npm run ball`) drives a real game in headless Chromium and reads back off the renderer what the ball is parented to and how far it is from the carrier's nearest hand — the one question the engine cannot answer, since it only knows who has possession. The running carry measured **0.33 yards**, a foot, and the quarterback's ready position 0.06. That split is the diagnosis: the ready grip hangs off `Socket_Hand_R`, a frame both rigs share, and the carry grip hangs off `LowerArm_*`, which they do not. It is the `restAlign` lesson a second time — the carry offsets are authored against the game's own player, whose forearm runs down its own -Y, and the character the game actually LOADS rests in an A-pose with `LowerArm_R -> Hand_R` at `[-0.207, -0.126, 0.019]`, so `[0, -0.27, -0.08]` pointed 0.27 in a direction that arm does not go and the ball hung below the elbow, held by nothing, on every carried frame of every play. The arm was already correct; nothing was carrying the BALL through the same constant. Median ball-to-hand **0.348yd -> 0.087yd**, worst 0.355 -> 0.114, and `ballcheck` now asserts on both bars — not in a hand at all, and held at arm's length, which the structural test could not see because the ball was parented to exactly the right limb the whole time. Renderer no-op on the game's own player, which carries no `restAlign`. Also, a correction to v3.9.0's flight trail found while verifying it: dropping samples by AGE bounded the streak's duration but not its sample count, so the 24-sample cap still bit above about 150fps and shortened the trail on a fast display — the same frame-rate dependence one level down. The head sample is now always "now" and a new one is COMMITTED behind it on a fixed clock, so span and count are both fps-independent: driven at 20/30/60/144/240fps the streak spans 0.133-0.154s throughout, where 240fps used to be clipped to 0.10s. |
| 2026-08-27 | `57c46ce3c8e096bb516d8c40741e294fd7099057` | **v3.9.0** — the ball you could not see. Reported from play: *"on low / short passes, we don't always see the ball move."* The obvious answer was wrong, and the new probe `tools/passstats.mjs` (`npm run passes`) is what ruled it out: **no pass in the game is on screen for fewer than eleven frames at 60fps**, and none is under six. What the buckets showed instead is geometry. A 3-6 yard pass rises **0.00 yards** and launches **2.4 degrees DOWNWARD** — it leaves at the ear and is caught at the chest, so it has to — and the camera sits behind the passer, which puts that flat flight almost entirely along the view axis where perspective foreshortens it away. Projected through the real camera at 720p, **a 3-yard pass moves 17 pixels from release to catch, and the ball is 11 pixels across**: it travels one and a half times its own width, and the eye reads that as a jump rather than a throw. The tempting fix was priced and rejected. Lofting short passes — a floor under the hang time so the passer takes something off a checkdown — needs 0.40s to produce visible arc, and that **costs six points of completion** (51.6% -> 45.4%), because `_aiDefender` sends all five at `ball.to` the moment the ball is airborne and 0.4s is 2.8 yards of defender against a 2.4-yard catch radius; a floor small enough to be nearly free (0.30s, about one point across three seeds) leaves the arc at 0.00 anyway. Both directions of that trade are bad, so the physics is untouched and the fix is a **flight trail**: one mesh, rebuilt from a ring buffer of the ball's recent world positions, tapering to nothing, facing the lens, drawn only while the ball is actually in the air. It turns two barely-separated positions into a continuous streak, which reads as motion at any length. The streak is a DURATION and not a sample count — 14 samples is 0.7s of history at 20fps and 0.12s at 120, so the same throw would leave a stripe across half the field on a slow machine and a stub on a fast one; samples carry their age and are dropped by it. Also, and found while measuring the first release frame: the ball used to teleport from the throwing hand to the passer's CENTRE at RELEASE_Z, a constant measured off the game's own rig — the Studio Ochi athlete's throwing hand is 11.5cm lower after the height normalisation, about half a ball-width of pop, plus a horizontal one. The renderer now eases the drawn ball off the socket it was actually in over 90ms. It reads the hand's real world position rather than a baked constant, so it needs nothing kept in step and works for a character nobody has imported yet; the engine's trajectory is not touched. Box score, rules and pursuit all unchanged — both changes are renderer-only. |
| 2026-08-27 | `9600cc89d126c08136709b633f6780e8cc0a778f` | **v3.8.0** — the bought character, on the field. The Studio Ochi athlete now IS the player: helmet, pads, cleats, ten men in team colours. What blocked it was never the skeleton, it was two assumptions the game makes about a rig and no purchased asset honours. (1) **Colour.** `playermodel.js` tints ten named material regions by multiplying `material.color` over white artwork; Ochi ships ONE material with the kit painted into an eight-swatch palette atlas, so there was nothing to tint. `tools/glb-repaint.mjs` regroups the triangles by which swatch they sample into one primitive per region, all still pointing at the SAME position, normal, joint and weight accessors — only the index buffer is new, so nothing can tear. 0.96% of texel samples are lost doing it, all of them the baked number 10, which cannot survive anyway: a tintable shirt is a shirt with no pixels of its own. (2) **Rest.** `rig-def.mjs` says "no bone carries a rest ROTATION" and the whole renderer leans on it — the chest number is hung off `Chest` at "plain metres relative to the chest joint", the ball is put in `Socket_Hand_R` at a fixed offset, a carrying arm is posed by writing euler triples straight onto `UpperArm_*`. A Rigify metarig is the opposite: 58 bones, every one carrying a real rest rotation. `tools/glb-rerig.mjs` rebuilds the character onto the game's conventions instead of adapting the game to it — every rest rotation removed at the position it already occupied, the mesh baked into that world space, every animation rewritten exactly (`W_new = W_old · conj(R)`), bones renamed, four sockets added, and it PROVES itself: the worst vertex moves 0.0287 mm across every clip, which is the six-decimal quantisation and nothing else. Where a rest DIRECTION still differs — this athlete stands in an A-pose, its upper arm 62° off — the correction is measured per bone into `extras.restAlign` and `field3d.js` composes it onto the poses it authors itself; it is absent on the game's own player, so nothing changes there. The 22 clips come from the game's OWN player, retargeted onto the metarig (`tools/mocap/ochi-clips.mjs`): a flag pull, a juke and ten celebrations exist in no capture library, free or paid. Two more things had to be true before it worked. A retarget carries angles, not contact — the walk came back with **53% flight**, which a walk does not have at all — so `tools/glb-ground.mjs` matches the reference's own foot-height profile per frame; and `playermodel.js` silently drops a gait rung with no measured `groundSpeed`, so `tools/glb-gait.mjs` measures it off the finished clips through a sole taken from the MESH, and reproduces the builder's own four numbers to **1.2%** from independent code and independent geometry. Also fixed, and older than any of this: **the rigged model only ever appeared if it loaded before the first snap.** Players are built once, and `Player3D.build` falls back to the procedural rig silently, so a kickoff that beat the fetch fielded the fallback for the whole game — invisible for as long as it was because `flagplayer.glb` is built from the same parts as that fallback and looks identical. `field3d` rebuilds when the model arrives now. The classic player stays, as the fallback when the imported asset is missing and behind the menu's Player toggle. |
| 2026-08-26 | `9600cc89d126c08136709b633f6780e8cc0a778f` | **v3.7.0** — nobody chased the runner. Reported from the game: *"defending players often just back up in front of a ball carrying attacker."* One question — *is the man with the ball a runner, or a passer still in the pocket?* — asked in two places with two different answers. `_aiQBOrCarrier` asks it correctly (`p === s.passer && !s.handoffDone`); `_aiDefender` asked the shorter, plausible, wrong `s.carrier !== s.passer`. They disagree on exactly one play, and it exists: `_doHandoff` on a play whose designed carrier is *already* the quarterback — **Flea Flicker**, the only one left since QB Keeper was deleted for being illegal — hands the ball to nobody and just sets `handoffDone`. The carrier IS the passer, and he is running. The carrier AI knew it and sent him downfield; the defence concluded there was no runner and left all five men in coverage for the whole play. Measured, **22% of every frame in the game that had a live runner in it had not one defender in pursuit**, and in those frames **65% of the defenders goal-side of the ball were drifting downfield** — a zone landmark lerps at most 0.7 of the way to the nearest receiver so it tracks without arriving, and man coverage sits 0.6yd goal-side, which downfield of a runner is a man walking backwards in front of him. Traced: a defender **0.9 yards in front of the ball, moving downfield at 3.5 yd/s for four seconds and twenty yards**, never turning to make the play. The question is asked once now, in `_isRunner`, and the three places that need it read it — including the rush branch, which had the same wrong test and so chased the runner's current position instead of solving the intercept B3 exists for. Frames with nobody in pursuit 21.7% → **0%**; goal-side backpedalling 35.7% → 22.7% (the residual is real cross-field pursuit — in the running lane it is 3.0%); closing 68.4% → **77.2%**. Those runs were unopposed and therefore enormous, so the box score moved with it: **yards per run 8.75 → 4.33** (target ~4-5), yards per pass play 6.09 → **7.04**, pull rate given contact 71.3% → **75.7%**, time-to-pull p90 3.48s → **2.68s**. New probe `tools/pursuitstats.mjs` (`npm run pursuit`) — the box score could not see this, so it classifies every defender-frame near a live runner by goal-side / backpedalling / closing and by which branch of `_aiDefender` he was in. |
| 2026-08-26 | `f138013af23650176a361121992c4d895b427c21` | **v3.6.0** — an arm inside a head, and a refusal shown once a frame. Two reports, both from watching the thing run. (1) The landing screen: **Lasso** swept `horiz` a full turn with `elev` held at 132, tracing a cone about the vertical through the shoulder — and the shoulder is 0.200m from the skull centre while the humerus is 0.335m, so the elbow spent part of every turn **61mm inside a sphere of radius 105mm**. Raising the arm cannot fix it (clearing the skull needs the elbow above 1.89m; a vertical humerus reaches 1.835m), so the circle is tilted outboard instead — elevation modulated by horiz, 118 outboard and 178 as it comes across: −61mm → **+63mm**. **FlagPulled** was the last clip still hand-typing euler triples at the shoulder, which stacked a **19.8° backward arch** and skated a planted foot at 2.52 m/s with a sole 13mm under the turf; re-authored through `armQ()`, lean never negative, skate 0.07 m/s. And the menu's green defender was playing `flagPull` → **FlagPulled**, the reaction of the man who LOST his flag, while the driver beside it threw a loose flag in the air at the rip — it plays `FlagGrab` now. The real gap was the instrument: every check in `measure-clip` asked about the ground, so a limb inside the body passed silently. It measures arm-vs-skull clearance now (segments, not just the hand — a forearm can cross a head with both ends outside it), with the skull defined once in `rig-def.mjs`; all 22 clips clear. (2) **"Only one forward pass per down!" was covering the catch announcement.** `_aiThrow` opens with `var qb = s.carrier` — whoever HAS the ball, which after a completion is the receiver — and `_dropback` calls it every frame for `s.passer`, so the engine asked to throw with a man who could not, once a frame, and `throwTo` answered by flashing a rules message at the player: measured, "Caught by Weber!" survived ONE frame and was buried for the other 52. Older than A9 — before it, the past-the-line message did the same. The engine does not ask now, so the refusal only ever answers a human press, and it reads **"Second forward pass not allowed!"** (#101) |
| 2026-08-26 | `d12ada92935e8d4e581a1515ea06a41f8d445dde` | **v3.5.0** — A10, the chains after a turnover past midfield. Four downs to cross midfield, three to score once you have — and both turnover paths set `crossedMid = false` unconditionally, asserting that the chains are still at midfield and this offence has to reach them. The takeover spot is `50 - yardsToGoal`, so whenever the team giving the ball up was in its own half the new offence starts PAST midfield and that line sits BEHIND the ball. `_advanceDown` tests `reachedMid = spotX >= MIDFIELD`, which a team starting past it satisfies on any snap at all, so the very next play awarded "First down — past midfield!" however it went: **a one-yard loss bought a fresh set of downs**. The HUD showed "1ST & 0" and the renderer drew the line-to-gain behind the offence. Measured over 8 games: 19.5% of possession changes take over past midfield, 24 of 25 left the chains unset, and 23 free first downs came out of it — about one turnover in five handing over an extra set, worth roughly a play a game. Both paths go through one `_takeOver` now, because the same three lines written twice is how one of them came to be wrong. Also E4, found while checking it: `ballOnText` was the NFL's 100-yard convention on a 50-yard field, so the OWN branch could never fire and every spot printed as "OPP" — your own 5 read "OPP 45" and midfield read "OPP 25", while the engine's own `_spotName` had been saying "own 5" and "midfield" in the flash message on top of it the whole time (#99) |
| 2026-08-25 | `4e06d885f0752bb8ce833c78c6be369f9e00c144` | **v3.4.0** — A9, one forward pass per down. Nothing in the engine recorded that a forward pass had happened, so nothing could refuse a second: the only guard in `throwTo` was positional, and refusing a throw from past the line happens to cover a receiver who catches it DOWNFIELD, which hid the hole completely. A screen, a swing or a checkdown is gathered BEHIND the line, and from there the ball could simply be thrown forward again. Not a human-only exploit either — **7.9% of CPU-vs-CPU plays contained more than one forward pass, up to three in a single down**, because `_dropback` runs for `s.passer` every frame while `handoffDone` is false (which on a pass play it always is) and `_aiThrow` opens with `var qb = s.carrier`, the man holding the ball now rather than the passer; the instant a completion was gathered behind the line the quarterback's own pocket logic threw it again through the receiver. Guarded at `throwTo`, the seam every forward pass goes through, and recorded at RELEASE rather than at the wind-up, since a wind-up the play moves out from under never becomes a pass — so handoffs still aren't passes and the RB Option Pass and Flea Flicker keep their one legal throw, and laterals stay unlimited. The box score never showed any of it and could not have: over five seeds either side, completions/yards-per-run/touchdowns all sit in overlapping ranges, because each individual throw is ordinary and it is the count per down that is illegal. Plays with more than one forward pass: 7.9% → 0%. Also the refusal message for the positional rule, which was misleading in the other direction — "No forward pass past the line!" reads as though the PASS may not travel past the line, i.e. that you cannot throw downfield at all, the opposite of the rule; it is the PASSER who must be behind it and it now says so, matching the A3 wording (#97) |
| 2026-08-25 | `6f9c5f4227c4218d8fccb9a6b4259258d966def3` | **v3.3.0** — pulling a flag took too long, and on Rookie it took longest for YOU. `tools/pullstats.mjs` measures the mechanic the box score kept blaming: on the default difficulty a pull needed a median 2.45s and 2.46 separate engagements, and 41% of the time a defender got a hand on the carrier and never got the flag at all. Three causes, all measured. A juke REWOUND the meter — the first of a play zeroed it outright, and since a carrier in trouble always has one available every engagement was erased once for free (41% of everything the defence ever filled); a juke's reward is that it ends the engagement, which was already modelled as separation plus 0.55s of stun, so it knocks the meter back 55% now rather than rewinding it. Losing contact reset rather than drained, at a flat 2.2/s against a fill near 0.79, so half a second of daylight erased a second and a half of work; it is proportional to the difficulty now and a grip already held keeps 0.35yd more reach than it needed to get, which took the share of close-quarters time actually spent grabbing from 68% to 77%. And `pullTime`/`jukeCd` were read off the difficulty preset for BOTH sides where `defSpeed` and `catchBonus` have always been read as `team !== userSide` — so Rookie, the setting that makes the game easy while you carry the ball, made your own defence *slower than the CPU's* the moment you didn't: 2.65s against their 1.37s. Your defence on Rookie now pulls in 0.90s, 88.5% of contact ends in a pull, and the ladder runs the right way (you 0.90 / 1.28 / 1.90 as the CPU goes 1.40 / 0.97 / 0.67). Plus a manual PULL out of range says so instead of doing nothing at all, and `tools/smoke.mjs`, the four-screens-two-orientations check CLAUDE.md has always asked for and never had a tool for (#95) |
| 2026-08-21 | `95a99024bb4a8f9af4cfb7834880ec39cf7a56b9` | **v3.2.0** — motion capture, retargeted. `tools/mocap/` carries CMU Graphics Lab .asf/.amc captures onto the 27-bone rig: one fixed quaternion per bone, with three exceptions that are about what "rest direction" means in each rig (their root is not a bone; aligning the foot bone rather than the sole would tilt ours 13° toe-down; honouring their clavicle would lift the shoulder 56mm). The pelvis is planted on the frames the SUBJECT had a foot down, not by hanging it off the lowest sole, which would re-plant a running clip in mid-air. Jog is now CMU 35_21 — 3.41 m/s, stance 38%, flight 25%, sweep even to 14% — and carries the arm a jogger actually carries. Walk was retargeted and deliberately not shipped; Sprint never will be, since CMU's capture volume is 3m x 8m. Generalising the measurement to a femur that abducts and twists also found a real error: read back through full quaternion FK the shipped clips disagreed with their own tables by 7%, because a planar solve cannot see a pelvis that yaws, lists and sways — the authored walk was holding its stance foot 5.5mm off the turf and measuring a 38% flight phase, which a walk does not have (#93) |
| 2026-08-21 | `01f52dc6fbfdfaf9b71e2fe7376ccf75b1f7b2f9` | **v3.1.0** — five celebrations that are not a man moving his arms. The end zone's existing five all differed in what the ARMS do and none in what the BODY does, which is the only thing that survives at the distance the game is seen from. The new ones each add an axis the set lacked: Bow is the only fold (69° of trunk flexion, measured), Lasso the only large moving element, Salute the only narrow one and the only man not waving, Griddy the only motion in the frontal plane below the pelvis, Point the first about the DOWN rather than the man. And the news stopped being told three identical ways — a touchdown gets the ridiculous end of the range, a takeaway the poses that hold still and stare, a first down a single beat from the carrier. Plus `tools/celebcheck.mjs` and `tools/posesheet.mjs` (#91) |
| 2026-08-19 | `5ae6c5b986b01cf5356e0bed71d7ba225ac3e9b0` | **v3.0.3** — the neck was never reset. No clip in the .glb animates the Neck bone (tracks exist for Hips, Spine, Chest and Head and none for Neck), so nothing rewrote it each frame and the gaze's `neck.quaternion.multiply(offset)` compounded on its own output every frame since v2.33: 88→93→98→103→108→113→119→124→128°, peaking at 179, while the offset being applied was eight. That is the spin, and it is why cutting the range twice changed nothing — a smaller offset only winds up more slowly. Composed from a cached rest pose now: rendered neck+head yaw p99 183.7°→42.4°, worst 218.4°→63.1°. Also, as asked, the head is a left-right joint only limited to 80° either side, with one writer, one budget and one clamp where three functions used to write it in yaw and pitch independently (#89) |
| 2026-08-19 | `f6c38c5126a77caae899cc5d7f6a808f5f83d2fd` | **v3.0.2** — a neck's range is a glance, not its anatomical limit. 3.0.1 fixed how fast the head turned and not how far: 70° is the full cervical spine at maximum strain standing still, and as a working range it put the head 63° off the chest at p99 and 65° at worst, held, while running, on a torso that was not turning with it. The layers had never been added up either — gaze took 43°, `leadTrunk` 15°, the clip about 9, and the neck another 27, each defensible alone with nothing owning the total. Cap cut to 29°, pitch to 14°, `leadTrunk`'s head share 0.85→0.55, and the summed total is published so the next layer can see what is already there: p99 32.7°, worst 34°. Also two probe corrections — the 941°/s reported last round was `animcheck` reading a between-plays player rebuild as a head snap (true peak 298, the rate cap holding), and the tracking score was counting a correctly-released neck as lag (#87) |
| 2026-08-19 | `e4e6f42f989f3f3328fb6fc4efcaa32e4c8d0bb3` | **v3.0.1** — heads were on a swivel. The gaze layer had clamped an off-range target to the nearest limit since v2.33, with a comment beside it saying it should do the opposite; with the ball BEHIND a player the bearing sits near ±180° and flips sign as it crosses, snapping the target between +70° and −70°. Peak head rate 1811 °/s against a human ceiling near 400, 2.5% of frames physically impossible. Now hysteresis decides whether to look (engage at 60°, give up at 80°, ease back to neutral rather than strain at a limit) and a rate cap decides how fast: peak 298 °/s, 0% impossible. `animcheck` had passed this while reporting excellent gaze lag and accuracy — neither can see a head that arrives correctly and violently — so it measures angular rate now (#85) |
| 2026-08-18 | `29109b795333a8a75a6bfaa1977478d831351c2a` | **v3.0.0** — the players stop being interchangeable. Fatigue reaches the body for the first time: stamina has been simulated for a long time (it drains, recovers, scales top speed to STAM_FLOOR and drives a HUD bar) and never once showed — measured at median 0.853 with 20.3% of a game below 0.7, so a fifth of every match was played by people visibly tiring and drawn fresh. The trunk folds, the head drops, the shoulders round and the arms stop being carried, with a deeper shape again for standing still and blown; the stride is left to the gait ladder, which already shortens it. And cloth that looks like cloth: a knit grid and low-frequency mottling for kit, low-frequency tonal variation for skin, both generated at load into shared canvases so no binary asset enters the repo and one upload serves twenty players (#83) |
| 2026-08-18 | `1deb24ff229adeb2b2d2080ac29e3f6214741210` | **v2.35.0** — the limp did not exist. Walk and Jog were reported limping on the strength of `measure-clip` putting right-foot contact at 40% and 38% against 50%; the gaits are built by rotating one leg's curve half a cycle and are symmetric to 0.00mm, so a limp was impossible. Three bugs in the measurement: the contact scan started on a planted foot and reported the frame it began on, a fixed 12mm threshold is meaningless during flight (the lower foot's height IS the pelvis-lift envelope), and the replacement check interpolated across a half-sample gap and invented up to 34mm. `measure-clip` reports symmetry directly now. What WAS real: Walk's swing foot floated 11mm at its nominal heel strike, so the rung's phase 0 was not contact — 1.5° of hip closed it, and the blender needs all four rungs to agree (#82) |
| 2026-08-18 | `fb86f856d301b50f0597df713059c5fe5c71ce0d` | **v2.34.0** — the lean is a cut, not a carve. Over running frames the bank was 9.9° median, pinned to its 15.5° ceiling at p90, past 8° for 56% of every play and held 0.6s at a time — a hockey rink. `atan(lateral/g)` is the angle that balances a *sustained* turn, which is skater physics; the force was low-passed over 0.28s; and the release was deliberately slower than the attack. Replaced with a washout, so the ONSET of a cut leans a body and merely being in a turn does not: 1.8° median, 0.20s hold (#81) |
| 2026-08-18 | `7f8156b5e491284f201ddd11bb58ae2b24c0d319` | **v2.33.0** — everyone watches the ball. There was no gaze behaviour at all: every head pointed wherever the body pointed, all game. A look-at layered over the mixer, split across neck and skull, eased and clamped at 70° — heads land within 12° of the ask 85% of the time. Plus an Idle that is no longer a photograph: its own comment said "the breathing is the only motion in here, and it is 12mm of it", in the pose the game spends most of its clock in. And `tools/animcheck.mjs`, which drops two of its own metrics for being untrustworthy (#80) |
| 2026-08-18 | `74eb5a30f14d9c15b7270e107baa4c02fb63c724` | **v2.32.0** — muscle, and light baked into it. Occlusion per-vertex into `COLOR_0` at two scales: short rays against the mesh for creases, analytic solid angle against a 40-blob proxy for bulk shading bulk. It barely showed at first, and that was the finding — occlusion needs concavity and a figure of convex tubes has none. `loft()` takes a per-angle radius modulation now, so a ring carries pectorals, lats, a spinal channel, trapezius, deltoid, quadriceps and calf without carrying vertices (#78) |
| 2026-08-16 | `23ae387afc601f976906728e3842189a2d29b888` | **v2.31.0** — flag guarding, which v2.17.0's release notes and REALISM.md both claimed had shipped and which had **no call sites at all**: `_flagGuard` sat in the engine for thirteen releases and never once fired. It is called now, off the only thing the game gives it to key on — breaking a grip is a cut and stays legal (29.7% of plays have one), while breaking the SAME defender's grip a second time in one play is the swat, and fires on 0.9% of plays, about the once a game a real match sees. Enforced as 10 yards from the spot of the foul and a loss of down, with the defence accepting or declining, and you cannot guard your way into the end zone. Plus `tools/ruletest.mjs`: 18 assertions on both fouls, because a box score cannot tell a rule that works from one that never runs |
| 2026-08-16 | `322c6c61be8277ee2c109bd91550e738e4143270` | **v2.30.0** — the illegal rush rebuilt. Every flag the game threw was a phantom: 3.9% of plays, all of them a linebacker in man coverage standing in the backfield because that is where his man went, not one of them a rusher. Eligibility is now stamped at the snap from where a player is actually standing rather than read off the play call (which waved through the called blitzer from six yards and criminalised the rusher on the line), crossing the line only counts when you are closing on the passer, and the flag no longer blows the whistle — the play runs to its end and the offence accepts or declines. Enforcement is five yards and an automatic first down instead of five yards and a lost down, which was strictly good for the defence on fourth down |
| 2026-08-15 | `b6b84aa16fd7759270b3dea774ce467f2da753c8` | **v2.29.0** — heads: the skull resculpted from a profile instead of a sphere equation (jaw, chin, brow, cheekbone), eyes/nose/mouth sculpted INTO the surface rather than stuck on it, hair as a shell following the skull with six switchable styles plus beards and an optional headband, a runtime face texture, and appearance seeded per player so ten players read as ten people — plus a gender flag threaded end to end for the women's tournament (#72) |
| 2026-08-14 | `3fd1eb2de55ced6210647ef36932847c682b9f9c` | **v2.28.2** — nobody stands inside anybody any more. `_separatePass` skipped any player whose flag had been pulled, on both sides of the pair, so the instant the pull landed the carrier left the collision set and the defenders converging on him walked straight through him — the one frame the camera settles on was the one frame the tackled man had no volume. He keeps it now, and takes none of the correction himself: he is an obstacle to go round, not a body to shove. Separation also ran below the `phase !== 'live'` return in `_update`, so any overlap standing at the end of a play was frozen for the whole dead ball; it now runs between plays too, position-only, because nobody is steering then and a velocity shove would drift the formation apart. Closest approach at a settled dead ball 0.841yd -> 0.900yd in a 0.900yd body, opponents within 1.6yd of the de-flagged man 4 -> 1, box score byte-identical over 48 games |
| 2026-08-14 | `0ca4e985b6b9ffaf591316c198285f73d70f94ee` | **v2.28.1** — the athletic stance v2.28.0 authored is finally the one you see. The clip was never wrong — measured off the shipped GLB it is knee 42/45, trunk lean 20.6, elbow 67 — it was being played at a weight of zero. Coming off the gait blend space `play()` did `setEffectiveWeight(0)` and then `fadeIn()`, but three.js computes effective weight as `weight x fade interpolant`, so the ramp multiplied zero and stayed there, and `setEffectiveWeight()` discards any fade already scheduled. A fresh model stands correctly because it has no gait mounted; the moment it runs once it comes back through that branch and renders in the BIND POSE for the rest of the game — arms straight down, knees locked, feet together, which is the planking. Ten players at a settled dead ball now measure hip/knee/spine/elbow -30/44/15/-68 against an authored -33/45/15/-67, where they read -1/3/0.4/-6 before |
| 2026-08-13 | `8350872472145188e16520795a69617e19326f3a` | **v2.28.0** — the players stop leaning like motorcycles and stop standing like celery, and a ball nobody caught falls over. The lean was `atan(lateral acceleration / g)` fed raw from a steering controller riding its own cross-acceleration limit, so the median player ran at a 12.6-degree bank with a THIRD of all frames past 20 and the peak pinned to the 26-degree clamp; the force is low-passed now and the ceiling is 15 degrees, which puts the median at 5.9 and the frames past 20 at zero. The Idle was a man standing to attention — feet together, knees locked, arms down the sides — and is now the stance the sport is played from, knees past 40 degrees, feet apart, hands up off the ribs, with each athlete starting somewhere of their own in the cycle so a formation is not ten men breathing in unison. An incompletion left the football hanging at catch height over an empty patch of turf, because the flight solver ends a pass at the height it would have been CAUGHT at and nothing carried on from there — it falls, bounces and lies where it stops. And a takeaway is celebrated by the side that made it: the man who picked it off is holding the ball, his four team-mates mob him, and the ball changes hands afterwards instead of during |
| 2026-08-13 | `6160c98c6604a5e14064cdb55b0900a186873bcf` | **v2.27.0** — the demo plays a real game and you can tell who has the ball. Watch Demo ran two 2-minute halves, about eight snaps: most demos never saw a touchdown, ended 0-0 and went to overtime, so the attract mode showed off the tie-breaker rather than football — it now plays the real IFAF game, two 20-minute halves, ~50 snaps. The second half never actually changed hands: the clock rolled over and whoever had the ball kept it, on the same down, at the same spot (twenty out of twenty second halves opened mid-drive), and overtime promised "alternating possessions from the 5" and then opened wherever regulation expired, ytg=34 on second down. The side that did not receive first now takes the second half on their own 5, overtime opens on the 5, and every change of possession is announced — after a score the flash said "conversion GOOD" and then a team, the other one, lined up on a 5-yard line in silence, which reads as the scoring side keeping the ball. And no confetti anywhere: not the five waves over a touchdown, not the puff on a first down, not the burst in the menu hero — the celebration is what the players do. (The jumbotron also still said Q1.) |
| 2026-08-13 | `6160c98c6604a5e14064cdb55b0900a186873bcf` | **v2.26.0** — the running rebuilt around the two things a still frame can't show. The `Run` clip had been swinging its arms a THIRD OF A CYCLE early for its whole life: every pose in it was a good pose, the arms swung the right distance at the right rate, and they arrived at the wrong time, which is what a wind-up toy is — now measured, per side, by `tools/measure-clip.mjs`. And a clip can only be played faster, so the old renderer got a receiver at 8.5yd/s by spinning one run cycle up to 465 steps a minute on a stride baked for a jogger; speed is stride length times stride frequency and both of them rise, so locomotion is now a four-rung ladder (Walk / Jog / Run / Sprint, two of them new) blended by the pair that brackets the player's speed, with the blend weight chosen so their measured ground speeds interpolate to it — playback rate then sits within 2% of 1.0 across the whole range instead of stretching to 2.4x, and cadence climbs 169→247 steps/min while the stride grows 2.56→4.09yd. The stride phase is driven rather than integrated, so it survives a rung change, a juke and a celebration without restarting from the left foot, and every athlete gets their own phase offset — ten men in a formation had been landing their left feet together for the whole play. Plus everything a change of direction does that was missing: the body banks into a turn by atan(lateral acceleration / g) up to 26 degrees, pivoting about the contact patch so the feet stay planted, leans forward under acceleration and back under braking, leads the cut with the head and shoulders before the hips, and turns harder the harder the cut — forward-running facing error down from 10.4 to 8.3 degrees mean and 3.5 to 0.8 median. Underneath: the builder now measures how EVEN a stance sweep is, not just how long (`Run` 43% spread -> 29%, `Jog` 42% -> 16%), and computes what a blend of two gaits really costs in ground speed and bakes the correction into the .glb |
| 2026-08-12 | `61605f60aae9f0c56a8530f805fef8afc0e55eb3` | **v2.25.0** — ships the v2.24.0 sideline camera fix, which had been sitting unmerged while the judder was still live, and divides the frame rate out of every visual lag filter: easing by `dt * rate` makes a filter's time constant a function of frame time, so a device that hitches moves the camera by different amounts on long frames than short ones. `1 - exp(-rate * dt)` halves the chatter a stutter adds (4.29% -> 2.01% variation in apparent pan speed against a constant-speed subject) and is identical to within 3% at 60fps |
| 2026-08-11 | `929262a37e430d90da2e867944f38e2ce4f8cf72` | **v2.24.0** — the players stop walking funny: the gait clips were interpolated LINEARLY between their authored phases, which puts a velocity corner in every joint at every phase boundary and clipped the peaks flat as well, and the rig's `Toe_*` joints had never been animated at all, so every foot in the game was one rigid plank from heel to tip. Rebuilt on a cyclic cubic through the same poses, with a two-segment foot that heel-strikes, rolls flat and leaves the ground over toes that stay put; the frontal plane is no longer frozen (pelvic drop toward the swing leg, lateral sway over the stance leg, shoulder counter-tilt, a head that stays pointed downfield), all derived from where the feet actually are rather than keyed by hand. Each gait's natural ground speed is now measured by the builder and baked into the .glb, which fixes a backpedal that had been borrowing the RUN's speed constant and skating backwards at a quarter rate for its whole life. Plus: the sideline camera judder — `keepInFrame` corrected in discrete thirds inside a loop whose other half eases back, i.e. a bang-bang controller, which limit-cycled several times a second whenever the carrier was wide (peak look-at judder 1.36 world units, mean 0.48; solving for the smallest correction instead gives 0.0027 and 0.00028). And an end zone worth scoring in: four new celebrations chosen per player so ten men aren't one clip in unison — a ball spike, a shimmy, a flex and a high-step — under five waves of confetti that drifts down through the whole shot instead of one puff that has landed in a second |
| 2026-08-11 | `ccbda0339924ad7a5df9983e00e629e6dbea0aad` | **v2.23.0** — the players celebrate, and the camera never loses the ball: a touchdown gets the whole scoring side jumping for nearly three seconds with the scorer spinning to the camera holding the ball aloft, three waves of confetti in the team's colours and a shockwave opening on the turf, while a midfield first down gets a beat of the same thing a third the size, from the carrier and whoever is standing near them; and the shot now follows the ball at every moment of the game — including a bouncing incompletion, which used to cut straight back to the old line of scrimmage — leading a runner by his own velocity and a pass by the catch point the engine already solved, with a hard guarantee that whatever holds the ball is inside the frame |
| 2026-08-11 | `202b37231e2a12a11bb95060a7f01ee317be74a7` | **v2.22.0** — the quarterback throws properly: the Throw clip re-authored against measured collegiate-QB kinematics and checked with a new `tools/measure-clip.mjs` (at the release instant the old one had the hand 0.41m BEHIND the chest, the trunk 67 degrees closed and the elbow at 95, and its fastest moment 0.18s after the ball had gone), and he turns to the receiver on the wind-up instead of after it. Plus: the camera sits behind whoever has the ball rather than behind the user; a defender reaches for the flag and rips it, and the carrier's flag actually comes off (`flagPulled` was never once set to true, so no flag pull had ever animated); arm strength reaches the ballistics (it was computed and then shadowed); and the rules — the regulation 70 x 30 field, out of bounds decided by crossing the line rather than coming within 0.4yd of it, a pass that can leave the field, three downs to score past midfield |
| 2026-08-11 | `c9afd5c6cfc0a83c7071f976fdabfe504b965ad5` | **v2.21.0** — the carrying arm runs: the ball-side arm pumps from the shoulder in step with the stride instead of being welded to a constant pose, with the elbow shut on a ball now tucked at the wrist and riding the ribs; and the passing game is repaired — the launch angle is solved between the real release and catch heights, so passes stop overshooting the receiver by 30% (completions 1.6% -> 53.8%) |
| 2026-08-10 | `05687b8205d7f9a98edd38235fb92a2382c6ff7d` | **v2.20.0** — the ball is part of the arm that holds it: parented to the forearm (or, for a passer, the throwing hand) instead of the chest, so the ball and the limb cannot come apart, with the arm pose rebuilt around it and the tuck dropped to waist height; and a hard guarantee that a pass stays on screen — the eased camera proposes a shot and is then corrected only as far as it takes to keep the ball inside the frame |
| 2026-08-10 | `64c71c3ba8257d1c30aa3c990fde316ccd23e255` | **v2.19.0** — you can always see the ball: a carry pose with an arm actually wrapped around it (it used to float at the armpit with the arm swinging through), a quarterback's ready position off the shoulder and into two hands, an occlusion ghost so a body in front of the ball no longer hides it, a real football texture, flight solved between release and catch height instead of ground-to-ground plus a fudge, a transfer tween so hand-offs and catches travel instead of teleporting, and a camera that keeps a sideline throw in frame |
| 2026-08-10 | `30e093f8b094fba0bc67022ef909f475bba4c950` | **v2.18.0** — the skate fixed at its source: separation resolved in velocity instead of teleporting bodies apart, animation driven by real displacement rather than intent, the carrier turning into their own juke, and the ball moved outside the ribs (it was inside the torso mesh) onto the arm away from the defender |
| 2026-08-10 | `343b2b4f0b234ea676b3d6e958074420ebd2cbe5` | **v2.17.0** — REALISM.md Phase 6 (clock, halves, penalties): two 20-minute halves with a continuously running clock, alternating-possession overtime, flag guarding, and real laterals — the last of the six phases. *(Correction, v2.31.0: flag guarding did not ship here. `_flagGuard` was written with no call sites and never fired.)* |
| 2026-08-10 | `f59df70bf4a0ea48d6717cceea9fc1c9c67a55df` | **v2.16.0** — REALISM.md Phase 5 (presentation): players are no longer ten copies of one body, the ball sits on the turf and is actually snapped, and the field has a spot and a down marker |
| 2026-08-10 | `d07abdf02733e964732b082e54d2c29f55be2479` | **v2.15.0** — REALISM.md Phase 4 (AI): coverage no longer empties on every throw, defenders hold leverage and can undercut a route, and the quarterback works a progression under pressure — interceptions off zero and into the 3-5% the sport runs at |
| 2026-08-10 | `138824a64a86bab02547157ec66af9677c1b74dd` | **v2.14.0** — REALISM.md Phase 3 (ball): real ballistics with gravity and hang time, a contested catch resolved in space instead of a coin flip, and the ball finally casts a shadow and spirals about its flight axis |
| 2026-08-09 | `06d78f79528a462b639b0dcba17c24c0d1114eca` | **v2.13.0** — REALISM.md Phase 2 (movement): acceleration and turn limits so players have mass, soft body separation, and pursuit that solves an intercept instead of chasing where the carrier used to be |
| 2026-08-09 | `92a7c025f5561ac10a4189d933fcb9ad73f186cc` | **v2.12.0** — REALISM.md Phase 1 (rules): 7-second pass clock, 7-yard rush line, the passer may not run, no-run zones, extra points as a real play, plus tools/simstats.mjs |
| 2026-08-09 | `176af3e40a171bf9df71f9a1e653fcc56ba8ef6f` | **v2.11.0** — the ball is carried by the player instead of floating near them: tucked high and tight on the outside arm off the chest bone, and riding the throwing hand through a wind-up |
| 2026-08-09 | `745131ce16f2307dbc53f477efa35ac676c598a7` | **v2.10.0** — no more skating: a player may face along their line of travel or along its reverse, never square across it, so legs always cycle in the direction the body is going |
| 2026-08-09 | `0cc4900b4c3590fbd91065d3e419951e383ba6ce` | **v2.9.0** — juke reworked (no more juke-lock, no cooldown burned on a whiff, a real sidestep), throws wind up so the ball leaves with the arm, and the CPU quarterback can finally pass at all |
| 2026-08-09 | `0111346b16523e61752e9d59fc289a1c81136527` | **v2.8.0** — the ball is regulation size instead of twice it, and the run cycle is stride-matched to ground speed with a longer stride, so feet plant instead of skating |
| 2026-08-09 | `a73fc4a3919275213a2ffe522621249c7d68a8fa` | **v2.7.0** — nobody freezes any more: receivers work open after their route, the centre releases instead of standing still, and zone/man/spy coverage tracks live targets instead of parking on a fixed dot |
| 2026-08-09 | `9bdbdc10e6f10d70f55b52083a8c8495439270a6` | **v2.6.0** — the running version is shown on the home screen, read from the VERSION file itself so it can't drift from what's deployed |
| 2026-08-09 | `01cffb2ae102abab864c9c791b65914a229e3c62` | **v2.5.0** — real jersey numbers by position (the renderer had been painting each player's OVR rating on their chest) and a safety only when your flag is pulled behind your own goal line, never for standing there |
| 2026-08-09 | `1f26722c7c368f849d1ec74f9a1f0ad83164b476` | **v2.4.0** — athletic player build (square shoulders, real neck, thicker arms and calves) and a portrait camera pulled back off the carrier, with the lens clamped inside the bowl |
| 2026-08-09 | `243074430c9bc2db549b82f9576abc015f5e60d5` | **v2.3.0** — broadcast presentation across the demo AND every played mode: a low over-the-shoulder chase camera in place of the fit-the-whole-field view, a scorebug/play-clock/situation HUD with procedural team crests and a field map, daylight relight with live jumbotrons, a real play clock and working stamina, and control hints, thumb-button clutter and floating nameplates taken off the field |
| 2026-08-08 | `ee8883d78e5fb19ca925c7332cf351fc88bd5194` | **v2.2.0** — slash to direct: draw a line across the field and your player runs the whole route hands-off, with the route painted on the turf |
| 2026-08-08 | `d44e50d5f9a89b7ed15c49193b6700bd3232397c` | **v2.1.1** — landing screen turned around: the cast now runs, cuts and throws toward the viewer, so you see faces and jersey fronts instead of three backs jogging away |
| 2026-08-08 | `8f8804f57a644dcea6ba8b997aee2a77d0d9c1d0` | **v2.1.0** — real running gait: knees and elbows bend the right way (they were inverted, which folded the foot behind the shin — the "ostrich run"), six-phase run/walk cycles, pelvis height solved by forward kinematics so feet plant instead of skating, landing screen waits for the rigged players, everyone faces the way they're running |
| 2026-08-08 | `323d1906391a50cdc257d1ed746f04994e6751df` | **v2.0.0** — real rigged/skinned/team-skinnable flag football player (glTF SkinnedMesh, 27 joints, 10 clips, 9 tintable regions) + reproducible generator; drops the 2.8MB Xbot (#34) |
| 2026-08-07 | `badcf47c09151d8f2d797b5f2971c819898d3dd5` | **v1.9.1** — fixed rotated control axes (right no longer goes down), tap a player to switch / throw, swipe to move (#32) |
| 2026-08-07 | `bedc17d4f761528f9f3a5c0b34684d925efed50d` | **v1.9.0** — Rookie/Pro/All-Pro difficulty (Rookie default), CPU-vs-CPU Watch Demo, contested flag pull with juke mechanic + grab meter (#30) |
| 2026-08-07 | `ee9ff9211e7eda1a8571ec634e1affc570cc560f` | **v1.8.0** — human-scale players (~6'2" instead of ~15ft), rig reoriented so limbs swing fore/aft (natural arms + stride), refreshed landing screen on the real stadium (#28) |
| 2026-08-07 | `bddb3464388181f4aa13aaa8f97d5985f0da30c3` | **v1.7.0** — Three.js r128→r185 via import map + guarded ESM bootstrap (2D fallback preserved), bloom/SMAA post-processing, colorSpace migration, lighting re-tuned for physically-correct units (#26) |
| 2026-08-07 | `2dcd368a81cfa1c4490f6fd655085a36fd890a24` | **v1.6.0** — Madden-style camera: always behind the team you play as, anchored behind the action, field width fills the frame (#24) |
| 2026-08-07 | `6df11a364453db54a6255a4ad9addeaa2062978f` | **v1.5.1** — crisper turf markings (mipmapped textures) (#22) |
| 2026-08-07 | `d11b13ecd1ce6782b0200c8be38764b2a5b5e429` | **v1.5.0** — professional broadcast presentation: procedural stadium (sky, crowd, floodlights), broadcast turf (yard numbers, hashes, lettered end zones), whole-field camera on any aspect, soft shadows + ACES tone mapping (#20) |
| 2026-07-21 | `bf76d1ab6c6b239709ca936323a2acd854d0e0c9` | **v1.4.1** — mobile swipe-to-move controls + centered (un-skewed) mobile camera (#18) |
| 2026-07-21 | `6447c6a990f53d0e95ae17b75949cfa2ce17b4cf` | **v1.4.0** — portrait field framing (fits horizontal field on tall screens) + men's flag-football player look (headband, broad shoulders, flag belt) (#16) |
| 2026-07-21 | `037017902a4b22e829fa7d2e862196258e8f8d6e` | **v1.3.0** — load a real rigged glTF character (GLTFLoader + Xbot), tinted per team, skeletal idle/walk/run in-game (#14) |
| 2026-07-21 | `c72c2d1aa16bf43f0dfb85e76bfddc2458899fe8` | **v1.2.0** — Madden behind-our-side camera + players idle between plays (#12) |
| 2026-07-21 | `e8b079ae1412b079b5411b086b86f033345f7fec` | **v1.1.0** — realistic rigged players + Three.js AnimationMixer/AnimationClip system (menu + in-game) (#10) |
| 2026-07-21 | `984ad93121f84de4b01d120b37eccc29e21888ce` | **v1.0.0** — football-like player stance & direction — jointed limbs, athletic poses, action-based facing (menu + in-game) (#8) |
| 2026-07-21 | `8be4df35bc3136c7206c5dc246400629dadc8479` | 3D in-game field (Three.js) + richer menu hero animations (#6) |
| 2026-07-21 | `c7204a8fb6f7074e18a0a976ab19ea7b4ec67626` | Three.js top-down 3D player animations on the menu + this deploy doc (#4) |
| 2026-07-21 | `d4cf30a30b46e2dd7d0249efb5a1cd2dda09ad87` | Replace Israel with Spain in the nations roster (#3) |
| 2026-07-20 | `58436bfbf73f1c5ab459f49eedda01cc3d58e11b` | Remove CNAME — site serves at alixpham.github.io (#2) |
| 2026-07-20 | `ae33742080bd49014bc0edeaf08a69b0ca5612e4` | Make Flagster the homepage (#1) |

> To find the current live commit at any time:
> `git rev-parse origin/master`
